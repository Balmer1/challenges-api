extends layout
block content
	div(id='topbar', class='navbar navbar-default')
		p
			a(href='/', id='title') [ API Project ]
			span(id='author') By Agustín Balmer

	div(id='main-full')
		p API: Timestamp microservice

		p User stories:
			ul
				li I can pass a string as a parameter, and it will check to see whether that string contains either a unix timestamp or a natural language date (example: January 1, 2016)
				li If it does, it returns both the Unix timestamp and the natural language form of that date.
				li If it does not contain a date or Unix timestamp, it returns null for those properties.
		p Example usage:
			br
			a(href='/timestamp/December%2015,%202015') https://freecodecamp-balmer.herokuapp.com/timestamp/December%2015,%202015
			br
			a(href='/timestamp/1450137600') https://freecodecamp-balmer.herokuapp.com/timestamp/1450137600
		p Example output:
			br
			| { "unix": 1450137600, "natural": "December 15, 2015" }